# Phase IV — dashboard

React app in `dashboard/`. Mockup PNG is in `assets/`, layout notes in `docs/`.

```bash
cd dashboard
npm install
npm run dev
```

Drop Phase III JSON into `dashboard/public/data/`. Sample `493626_criticality.json` is already there.

Satellite background is optional — add `{id}_sat.jpg` to the same folder if you want it.
