# Phase III — criticality

Notebook: `phase3_criticality.ipynb`

Picks up `graphs_deeplab/*.json`, runs betweenness centrality, removes top nodes one by one, exports JSON + PNG for the dashboard.

CPU only. Copy the `*_criticality.json` files into `phase4/dashboard/public/data/` when you're done.
